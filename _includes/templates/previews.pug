//- mixin tags(tags)
//-   div(class="div inline")
//-     ul
//-       for tag in tags
//-         li: a(class= 'tag', href= '/' + tag.toLowerCase())
//-           = tag

mixin previewlist(which, ...more)
  ul
    for elem in search.pages(which, more)
        +preview(elem)

mixin preview(post)
  - var raw_date = `${post.data.date}`
  include /templates/fdate_input.pug
  include /templates/tags.pug
  section
    a(href=post.data.url) #{post.data.title} - #{fdate}
    +list_tags(post.data.tags)
